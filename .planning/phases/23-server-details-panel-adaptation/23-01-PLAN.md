---
phase: 23-server-details-panel-adaptation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Clicking a protocol server card opens the details panel with full server info"
    - "Clicking a NAS table row opens the same details panel with full server info"
    - "Details panel shows: name, protocol, health, ports, endpoints, and actions for NAS servers"
  artifacts:
    - path: "src/dashboard/src/components/NasTable.tsx"
      provides: "Row click handler calling onRowClick(server)"
      contains: "onRowClick(server)"
    - path: "src/dashboard/src/components/ServerGrid.tsx"
      provides: "Passes onCardClick as onRowClick to NasTable"
      contains: "onRowClick={onCardClick}"
    - path: "src/dashboard/src/components/ServerDetailsPanel.tsx"
      provides: "Full details panel for any protocol including NFS"
      contains: "getProtocolInfo(server.name, server.protocol)"
    - path: "src/dashboard/src/utils/protocolInfo.ts"
      provides: "NFS protocol info with display name, ports, config"
      contains: "case 'NFS'"
  key_links:
    - from: "src/dashboard/src/components/NasTable.tsx"
      to: "src/dashboard/src/components/ServerGrid.tsx"
      via: "onRowClick prop called on row body click"
      pattern: "onClick=.*onRowClick\\(server\\)"
    - from: "src/dashboard/src/components/ServerGrid.tsx"
      to: "src/dashboard/src/App.tsx"
      via: "onCardClick={setSelectedServer} passed to ServerGrid"
      pattern: "onCardClick=.*setSelectedServer"
    - from: "src/dashboard/src/App.tsx"
      to: "src/dashboard/src/components/ServerDetailsPanel.tsx"
      via: "selectedServer state passed as server prop"
      pattern: "server=.*selectedServer"
---

<objective>
Verify that the NAS row click to details panel integration is fully working and that the
ServerDetailsPanel displays all required information for NFS servers.

Purpose: Phase 22 (NAS Compact Table View) already wired NasTable row clicks through to
setSelectedServer in App.tsx, and ServerDetailsPanel already handles NFS protocol servers.
This plan verifies end-to-end correctness rather than building new features.

Output: Verification that all three success criteria are met, plus a SUMMARY documenting
the existing integration. No code changes expected unless verification reveals a gap.
</objective>

<execution_context>
@C:/Users/UserC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/UserC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-nas-compact-table-view/22-02-SUMMARY.md
@src/dashboard/src/components/NasTable.tsx
@src/dashboard/src/components/ServerGrid.tsx
@src/dashboard/src/components/ServerDetailsPanel.tsx
@src/dashboard/src/utils/protocolInfo.ts
@src/dashboard/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify NAS-to-details-panel wiring and NFS display completeness</name>
  <action>
Verify the full click-to-panel chain is wired correctly and NFS servers display all required
information in the details panel. This is a verification task, not an implementation task.

VERIFICATION STEPS (read files and confirm each):

1. **NasTable row click handler** -- Read NasTable.tsx and confirm:
   - Row body div has `onClick={() => onRowClick(server)}` (line ~160)
   - Chevron click uses `e.stopPropagation()` so expand does NOT trigger row click
   - `onRowClick` prop is typed as `(server: ServerStatus) => void`

2. **ServerGrid passes onCardClick to NasTable** -- Read ServerGrid.tsx and confirm:
   - NasTable receives `onRowClick={onCardClick}` (line ~78)
   - Same `onCardClick` callback used for both ServerCard onClick and NasTable onRowClick

3. **App.tsx wires setSelectedServer** -- Read App.tsx and confirm:
   - ServerGrid receives `onCardClick={setSelectedServer}` (line ~492)
   - ServerDetailsPanel receives `server={selectedServer}` (line ~559-560)
   - Panel close sets `setSelectedServer(null)` (line ~561)

4. **protocolInfo.ts handles NFS** -- Read protocolInfo.ts and confirm:
   - `case 'NFS'` returns displayName "NFS Server", defaultPort 2049, nodePort 32049
   - Config includes Export Path, NFS Version, Sync Mode, Access
   - No credentials (NFS does not use username/password) -- this is correct

5. **ServerDetailsPanel displays all required fields for NFS** -- Read ServerDetailsPanel.tsx and confirm:
   - **Name**: Rendered in h3 element (line ~313)
   - **Protocol**: `protocolInfo.displayName` shown in panel-protocol span (line ~315)
   - **Health**: Health state with colored badge (lines ~344-347)
   - **Ports**: Port and NodePort fields (lines ~417-424)
   - **Endpoints**: Cluster internal and external Minikube URLs with copy buttons (lines ~411-416)
   - **Actions**: Lifecycle buttons shown when `isDynamic` (lines ~362-388)
   - **Storage**: Directory section shown when `server.directory` exists (lines ~428-441) --
     NAS servers always have `directory` populated
   - **Configuration**: Helm read-only config section shows NFS-specific items from
     protocolInfo.config (lines ~461-474)

6. **Build verification** -- Run `npm run build` in src/dashboard to confirm no TypeScript
   or build errors exist.

IF ANY GAP IS FOUND during verification:
- Fix the gap (e.g., missing NFS handling in protocolInfo, broken wiring)
- Document what was fixed and why
- Ensure build still passes after fix

IF NO GAPS FOUND (expected outcome):
- Document that all wiring is complete and working
- Note this was a verification-only phase -- Phase 22 already completed the integration
  </action>
  <verify>
Run `npm run build` in src/dashboard directory -- must exit 0 with no errors.

Additionally, verify by reading the source files that:
- NasTable.tsx line ~160 has `onClick={() => onRowClick(server)}`
- ServerGrid.tsx line ~78 has `onRowClick={onCardClick}`
- App.tsx line ~492 has `onCardClick={setSelectedServer}`
- App.tsx line ~559 has `server={selectedServer}` on ServerDetailsPanel
- protocolInfo.ts has a `case 'NFS'` returning displayName "NFS Server"
  </verify>
  <done>
All three Phase 23 success criteria confirmed:
1. Protocol server cards open details panel (existing behavior, verified via onCardClick chain)
2. NAS table rows open details panel (verified via onRowClick -> onCardClick -> setSelectedServer)
3. Details panel shows name, protocol, health, ports, endpoints, and actions for NFS servers
Build passes with no errors.
  </done>
</task>

</tasks>

<verification>
Phase 23 success criteria (from ROADMAP.md):
1. Clicking a protocol server card opens the details panel (existing behavior preserved)
2. Clicking a NAS table row opens the same details panel with full server info
3. Details panel shows: name, protocol, health, ports, endpoints, and actions

All three are satisfied by existing code from Phase 22 and earlier phases. This plan
verifies that integration and documents it.
</verification>

<success_criteria>
- Dashboard builds successfully (`npm run build` exits 0)
- All wiring verified: NasTable -> ServerGrid -> App -> ServerDetailsPanel
- NFS protocol info confirmed: display name, ports, config, no credentials
- All required panel fields confirmed present for NFS servers
- SUMMARY created documenting Phase 23 as a verification-only phase
</success_criteria>

<output>
After completion, create `.planning/phases/23-server-details-panel-adaptation/23-01-SUMMARY.md`
</output>
