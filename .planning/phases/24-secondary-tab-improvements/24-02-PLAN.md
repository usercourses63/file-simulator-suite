---
phase: 24-secondary-tab-improvements
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dashboard/src/App.css
  - src/dashboard/src/components/KafkaTab.css
  - src/dashboard/src/components/AlertsTab.css
autonomous: true

must_haves:
  truths:
    - "Files tab sidebar is 400px wide (was 350px)"
    - "Kafka side panels use 300px+ width on wide viewports"
    - "Alerts table title and message columns use flexible widths without hardcoded max-widths"
  artifacts:
    - path: "src/dashboard/src/App.css"
      provides: "Files sidebar increased to 400px"
      contains: "400px"
    - path: "src/dashboard/src/components/KafkaTab.css"
      provides: "Wider Kafka side panels"
      contains: "minmax"
    - path: "src/dashboard/src/components/AlertsTab.css"
      provides: "Flexible alert column widths"
  key_links:
    - from: "src/dashboard/src/App.css"
      to: ".files-container grid"
      via: "grid-template-columns"
      pattern: "grid-template-columns.*400px"
    - from: "src/dashboard/src/components/KafkaTab.css"
      to: ".kafka-layout grid"
      via: "grid-template-columns with minmax"
      pattern: "grid-template-columns.*minmax"
---

<objective>
Adjust layout widths for Files, Kafka, and Alerts tabs to improve readability and utilize viewport space.

Purpose: Wider sidebars show more file path text, wider Kafka panels reduce truncation, flexible alert columns prevent cut-off titles/messages.
Output: Updated App.css, KafkaTab.css, AlertsTab.css
</objective>

<execution_context>
@C:/Users/UserC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/UserC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/dashboard/src/App.css
@src/dashboard/src/components/KafkaTab.css
@src/dashboard/src/components/AlertsTab.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Increase Files sidebar width and widen Kafka side panels</name>
  <files>src/dashboard/src/App.css, src/dashboard/src/components/KafkaTab.css</files>
  <action>
In App.css (around line 937):
1. Change the `.files-container` grid-template-columns from `1fr 350px` to `1fr 400px`:
   ```css
   .files-container {
     display: grid;
     grid-template-columns: 1fr 400px;
     gap: 1rem;
     height: calc(100vh - 120px);
   }
   ```
   The mobile breakpoint at 1024px (line 956) remains unchanged (already collapses to single column).

In KafkaTab.css (around line 49):
1. Change the `.kafka-layout` default grid from `280px 1fr 280px` to use `minmax(280px, 320px)` for the side panels:
   ```css
   .kafka-layout {
     display: grid;
     grid-template-columns: minmax(280px, 320px) 1fr minmax(280px, 320px);
     gap: var(--spacing-lg);
     flex: 1;
     min-height: 0;
   }
   ```
   This gives side panels 280-320px depending on available space. At typical 1920px width, they will get ~320px.

2. Update the 1200px media query breakpoint to reduce to `250px 1fr 250px` (same as before, no change needed -- already correct).

3. The 1024px breakpoint remains unchanged (single column).
  </action>
  <verify>Run `cd src/dashboard && npm run build` -- no build errors.</verify>
  <done>Files sidebar is 400px wide. Kafka side panels use 280-320px depending on viewport width.</done>
</task>

<task type="auto">
  <name>Task 2: Remove hardcoded max-widths from Alerts table columns</name>
  <files>src/dashboard/src/components/AlertsTab.css</files>
  <action>
In AlertsTab.css:
1. Remove `max-width: 250px` from `.alerts-tab__title` (around line 145-148). Replace with flexible sizing:
   ```css
   .alerts-tab__title {
     font-weight: 600;
   }
   ```
   Simply remove the max-width constraint. The title column will naturally size based on content within the table layout.

2. Remove `max-width: 400px` from `.alerts-tab__message` (around line 150-155). Keep the other properties:
   ```css
   .alerts-tab__message {
     color: var(--text-secondary);
     white-space: normal;
     word-break: break-word;
   }
   ```
   This allows the message column to grow with the wider layout. `word-break: break-word` still prevents extremely long strings from blowing out the table.
  </action>
  <verify>Run `cd src/dashboard && npm run build` -- no build errors.</verify>
  <done>Alert table title and message columns use flexible widths that grow with the viewport instead of being capped at 250px and 400px.</done>
</task>

</tasks>

<verification>
1. `cd src/dashboard && npm run build` completes without errors
2. Manual verification: Files tab sidebar is visibly wider (~400px)
3. Manual verification: Kafka side panels are wider on large viewports (300px+)
4. Manual verification: Alerts table title and message columns expand to fill available space
5. All tabs still collapse properly at 1024px breakpoint
</verification>

<success_criteria>
- Files tab sidebar width is 400px (FILES-01)
- Kafka side panels use 300px+ width on wide viewports (KAFKA-01)
- Alerts table columns use flexible widths without hardcoded max-widths (ALERTS-01)
- No CSS layout regressions at narrow viewports
</success_criteria>

<output>
After completion, create `.planning/phases/24-secondary-tab-improvements/24-02-SUMMARY.md`
</output>
