# PersistentVolumeClaim for NAS Input Server 2
#
# Purpose: Claim nas-input-2-pv for application use
# Binds to: nas-input-2-pv (via selector nas-server: nas-input-2)
#
# Usage:
#   1. Adjust namespace to your application's namespace
#   2. kubectl apply -f nas-input-2-pvc.yaml -n <your-namespace>
#   3. Reference in deployment: claimName: nas-input-2-pvc
#
# Integration Guide: See ../docs/NAS-INTEGRATION-GUIDE.md

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nas-input-2-pvc
  namespace: default  # Change to your application namespace
  labels:
    nas-server: nas-input-2
    nas-role: input
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  selector:
    matchLabels:
      nas-server: nas-input-2  # Binds to nas-input-2-pv
